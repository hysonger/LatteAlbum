# =============================================================================
# Latte Album 统一部署配置
# =============================================================================
# 复制此文件为 .env 并修改配置

# -----------------------------------------------------------------------------
# 服务配置
# -----------------------------------------------------------------------------
# 服务端口 (后端服务端口，也是前端访问端口)
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# 开发环境配置
# -----------------------------------------------------------------------------
# 前端开发服务器端口
DEV_SERVER_PORT=3000

# -----------------------------------------------------------------------------
# 文件路径配置
# -----------------------------------------------------------------------------
# 照片目录路径 (必需) - 支持 NAS 挂载路径
# 示例: /mnt/nas/photos 或 //wsl$/Ubuntu/mnt/c/photos
ALBUM_PHOTOS_PATH=/mnt/nas/photos

# 缩略图缓存目录 (Docker volume 管理)
# 注意: 改用 Docker volume (latte_cache)，无需手动指定路径

# 数据库 (Docker volume 管理)
# 注意: 改用 Docker volume (latte_db)，无需手动指定路径

# -----------------------------------------------------------------------------
# JVM 配置
# -----------------------------------------------------------------------------
# 根据 NAS 内存大小调整
JAVA_OPTS=-Xmx2g -Xms512m
