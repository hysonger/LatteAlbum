# =============================================================================
# 远程服务器部署配置
# =============================================================================
# 复制此文件为 .env.remote 并修改配置
# =============================================================================

# -----------------------------------------------------------------------------
# 远程服务器配置 (必需)
# -----------------------------------------------------------------------------
# SSH 用户名
REMOTE_USER=root

# SSH 主机地址 (IP 或域名)
REMOTE_HOST=192.168.1.100

# SSH 端口 (默认 22)
REMOTE_PORT=22

# -----------------------------------------------------------------------------
# 镜像配置
# -----------------------------------------------------------------------------
# 镜像名称
IMAGE_NAME=latte-album

# 镜像标签
TAG=latest

# -----------------------------------------------------------------------------
# 远程服务配置
# -----------------------------------------------------------------------------
# 服务端口 (外部访问端口)
REMOTE_SERVER_PORT=8080

# 远程服务器上的照片目录路径 (必需)
# 示例: /mnt/docker/photos 或 /home/user/photos
REMOTE_ALBUM_PHOTOS=/mnt/docker/photos

# JVM 内存配置
JAVA_OPTS=-Xmx2g -Xms512m
