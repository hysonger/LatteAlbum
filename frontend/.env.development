# Development environment variables
# Used by Vite dev server

# Backend port for proxy (matches SERVER_PORT in root .env)
VITE_BACKEND_PORT=8080

# Frontend dev server port
VITE_DEV_SERVER_PORT=3000
