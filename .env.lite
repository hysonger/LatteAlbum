# Latte Album 轻量级部署配置
# 复制此文件为 .env.lite 并修改配置

# 远程服务器配置（必需）
REMOTE_HOST=192.168.66.66
REMOTE_USER=songer
REMOTE_PORT=678

# 部署路径（可选，默认 /opt/latte-album）
REMOTE_PATH=/opt/latte-album

# 服务用户（可选，默认 latte）
# 如果设置为空或当前用户，将使用当前用户运行服务
SERVICE_USER=latte

# 服务端口（可选，默认 8080）
SERVER_ADDRESS=192.168.66.66
SERVER_PORT=9292

# 应用配置（可选）
# 照片目录路径
ALBUM_BASE_PATH=/mnt/localmedia/photos/

# 缓存目录（相对于部署路径）
ALBUM_CACHE_DIR=./cache

# 数据库路径（相对于部署路径）
ALBUM_DB_PATH=./data/db/database.db

# JVM 参数（可选）
JAVA_OPTS="-Xmx2g -Xms512m"
